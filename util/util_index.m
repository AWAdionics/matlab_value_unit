function util_index()
    %util_index index of utility functions
    %
    %
    % Char operators
    %   - char_quick_split (fast splitter)
    % Excel Handlers
    %   - entry_excel (entry->excel)
    %   - excel_entry (excel->entry)
    %   - excel_find_col (find colum of entry)
    %   - excel_find_nonzero_rows_in_col (find non zero rows)
    % Map operators (mostly defunct)
    %   - combine_map_cell (combines maps and cells)
    %   - combine_maps (combines maps)
    %   - map_not_keys (finds keys not in map)
    %   - map_order_switch (switches order of nested maps)
    %   - map_vec (turns a map into a vector)
    %   - vec_map (turns a vector into a map)
    % .Mat Handlers
    %   - mat_var (loads a .mat)
    %   - var_mat (saves a .mat)
    % Struct Opterators
    %   - combine_structs (combines)
    %   - keyvals_struct ({keys}{values} -> struct)
    %   - struct_find_key_from_val (find key of val in struct)
    %   - struct_keys (get keys of struct)
    %   - struct_vals (get values of struct)
    %   - structnum (format number to be suitable struct key)
end